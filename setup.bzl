# load the pinned Maven artifacts
# must be done after repositories.bzl

def bazel_maven_dependencies_setup():
    """An utility method to perform setup work.

    It doesn't do anything at the moment.
    """
    pass
